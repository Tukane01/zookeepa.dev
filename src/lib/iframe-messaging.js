// Logic for handling communication with iframes

/**
 * A no-op helper used by the app to ensure we can safely call iframe messaging setup.
 * Add real iframe messaging logic here if you embed the app in an iframe and need to
 * listen for messages / post messages back to a parent frame.
 */
export function setupIframeMessaging() {
  // No-op for now.
}
